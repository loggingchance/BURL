export const roundBoardFeet = (n: number) => Math.round(n);
export const roundCords = (n: number, decimals = 3) => parseFloat(n.toFixed(decimals));
export const roundTons = (n: number) => parseFloat(n.toFixed(2));
